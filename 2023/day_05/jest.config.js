module.exports = {
  preset: 'ts-jest',
  testEnvironment: 'node',
  // This tells Jest where to look for tests
  //roots: ['<rootDir>/2023/day_05/test'],
  roots: ['<rootDir>/test'],
  // Automatically clear mock calls and instances between every test
  clearMocks: true,
};

